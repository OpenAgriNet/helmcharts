apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "decision-support-system.envConfigMapName" . }}
  labels:
    {{- include "decision-support-system.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- with (include "common.envConfigMapData" . | trim) }}
data:
  {{- . | nindent 2 }}
{{- else }}
data: {}
{{- end }}
