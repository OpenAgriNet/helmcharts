apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "knowledge-provider-ui.envConfigMapName" . }}
  labels:
    {{- include "knowledge-provider-ui.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  {{- include "common.envConfigMapData" . | trim | nindent 2 }}
