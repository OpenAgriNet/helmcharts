apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "knowledge-provider-api.envConfigMapName" . }}
  labels:
    {{- include "knowledge-provider-api.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  {{- include "common.envConfigMapData" . | trim | nindent 2 }}
