Thank you for installing {{ .Chart.Name }} (release: {{ .Release.Name }}).

Bharat Vistaar Docs Pipeline Temporal (temporalio/auto-setup). Internal-only -
never exposed outside the cluster.

Resources in namespace "{{ .Release.Namespace }}":
  - Deployment/{{ include "knowledge-provider-temporal.fullname" . }} ({{ .Values.replicaCount }} replica(s))
  - Service/{{ include "knowledge-provider-temporal.fullname" . }} ({{ .Values.service.type }} on port {{ .Values.service.port }})
  - ConfigMap/{{ include "knowledge-provider-temporal.envConfigMapName" . }}
{{- if .Values.serviceAccount.enabled }}
  - ServiceAccount/{{ include "knowledge-provider-temporal.serviceAccountName" . }}
{{- end }}

Image in use:
  {{ include "knowledge-provider-temporal.image" . }}
{{- if not .Values.image.repository }}

WARNING: image.repository is empty. Set image.registry/repository/tag before a
real deployment, e.g. --set image.repository=temporalio/auto-setup
{{- end }}

Check the rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "knowledge-provider-temporal.fullname" . }}

Reach it in-cluster (gRPC frontend):
  {{ include "knowledge-provider-temporal.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
