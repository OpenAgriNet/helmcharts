apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-provider-temporal.fullname" . }}
  labels:
    {{- include "knowledge-provider-temporal.labels" . | nindent 4 }}
  {{- with (merge (dict) .Values.service.annotations (.Values.commonAnnotations | default dict)) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "knowledge-provider-temporal.selectorLabels" . | nindent 4 }}
