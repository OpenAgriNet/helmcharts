apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "knowledge-provider-temporal.envConfigMapName" . }}
  labels:
    {{- include "knowledge-provider-temporal.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  {{- include "common.envConfigMapData" . | trim | nindent 2 }}
