apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "template.envConfigMapName" . }}
  labels:
    {{- include "template.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  {{- include "common.envConfigMapData" . | trim | nindent 2 }}
