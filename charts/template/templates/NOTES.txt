Thank you for installing {{ .Chart.Name }} (release: {{ .Release.Name }}).

Rendered from the template reference chart.

Resources in namespace "{{ .Release.Namespace }}":
  - Deployment/{{ include "template.fullname" . }} ({{ .Values.replicaCount }} replica(s))
  - Service/{{ include "template.fullname" . }} ({{ .Values.service.type }} on port {{ .Values.service.port }})
  - ConfigMap/{{ include "template.envConfigMapName" . }}
{{- if .Values.serviceAccount.enabled }}
  - ServiceAccount/{{ include "template.serviceAccountName" . }}
{{- end }}
{{- if .Values.ingress.enabled }}
  - Ingress/{{ include "template.fullname" . }}
{{- end }}

Image in use:
  {{ include "template.image" . }}
{{- if not .Values.image.repository }}

WARNING: image.repository is empty. Set image.registry/repository/tag before a
real deployment, e.g. --set image.repository=openagrinet/my-service
{{- end }}

Check the rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "template.fullname" . }}

Reach the service in-cluster:
  {{ include "template.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
